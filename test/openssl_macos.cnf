# OpenSSL Engine Configuration for macOS Local Build Testing
# This config is for testing the locally built engine on macOS

# Load the SM engine
openssl_conf = openssl_init

[openssl_init]
engines = engine_section

[engine_section]
sm_ce_engine = sm_section

[sm_section]
engine_id = sm_ce_engine
# Path relative to build directory on macOS
dynamic_path = ./lib/libsm_engine.dylib
init = 1
# 让引擎接管摘要和对称加密算法
default_algorithms = DIGESTS,CIPHERS